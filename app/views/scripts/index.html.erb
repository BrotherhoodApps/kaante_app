<h1>Listing scripts</h1>

<table>
  <tr>
    <th>Dialog</th>
    <th>Character</th>
    <th>Author</th>
    <th>Uprating</th>
    <th>Downrating</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @scripts.each do |script| %>
  <tr>
    <td><%= script.dialog %></td>
    <td><%= script.character %></td>
    <td><%= script.author %></td>
    <td><%= script.upRating %></td>
    <td><%= script.downRating %></td>
    <td><%= link_to 'Show', script %></td>
    <td><%= link_to 'Edit', edit_script_path(script) %></td>
    <td><%= link_to 'Destroy', script, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Script', new_script_path %>
